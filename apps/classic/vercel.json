{
  "installCommand": "npm install --prefix ../..",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://mprofit-replica-api.onrender.com/api/$1" }
  ]
}
